rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All database access goes through Next.js API routes (Firebase Admin SDK).
    // Block direct client reads/writes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
